const { Builder } = require('selenium-webdriver'); const chrome = require('selenium-webdriver/chrome'); async function run() { const options = new chrome.Options(); options.setMobileEmulation({ deviceName: 'Pixel 5' }); options.setChromeBinaryPath('C:/Program Files/Google/Chrome/Application/chrome.exe'); options.addArguments('--headless=new'); const driver = await new Builder().forBrowser('chrome').setChromeOptions(options).build(); await driver.get('http://google.com'); console.log('success'); await driver.quit(); } run();